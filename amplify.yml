version: 1
frontend:
  phases:
    build:
      commands:
        - sed -i "s|__API_URL__|$API_URL|g" frontend/*.html
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
